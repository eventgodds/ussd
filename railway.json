{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:${PORT:-8080} index.php",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
